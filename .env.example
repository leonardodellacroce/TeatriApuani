# Copia questo file in .env.local e compila i valori.
# NON committare .env.local (è già in .gitignore)

# Database - per produzione usa PostgreSQL (Neon)
# Per sviluppo locale con SQLite: non serve, usa prisma/schema.prisma con provider sqlite
# Per sviluppo locale con PostgreSQL: usa la connection string del progetto Neon "dev"
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# NextAuth - obbligatorio per login
NEXTAUTH_SECRET="genera con: openssl rand -base64 32"
NEXTAUTH_URL="http://localhost:3000"

# Opzionali
# IP_SALT="stringa per firma IP nei documenti"
# APP_ORIGIN="http://localhost:3000"
